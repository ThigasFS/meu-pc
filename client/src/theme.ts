import { createTheme } from "@mui/material"

export const theme = createTheme({
    palette: {
        mode: "dark",

        primary: {
            main: "#55E6B4"
        },

        background: {
            default: "#050505",
            paper: "#101010"
        }
    }
})